"""PerfPilot Agents - AG2-based AI agent framework for the MCP Perf Suite.

The folder name on disk is `agent-framework/` (with hyphen) for engineering
clarity; the user-facing brand is "PerfPilot Agents". Per Python module-naming
rules the hyphenated folder is not directly importable as a package - the
top-level packages used by the runtime are `agents/`, `utils/`, `workflows/`,
and `frontend/`. See AGENTS.md for the folder map.
"""

__version__ = "0.1.0"
